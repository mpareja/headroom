{
  "name": "headroom",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "engines": {
    "node": ">=8",
    "npm": ">=3"
  },
  "nyc": {
    "statements": 100,
    "branches": 100,
    "functions": 100,
    "lines": 100,
    "cache": true,
    "check-coverage": true,
    "reporter": [
      "lcov",
      "text-summary"
    ]
  },
  "scripts": {
    "clean": "rimraf .nyc_output coverage",
    "cov": "opener coverage/lcov-report/index.html",
    "lint": "standard --env mocha",
    "test": "npm run clean && npm run lint && npm run security && nyc npm run test-only",
    "test-only": "mocha --recursive --reporter ${MOCHA_REPORTER:-spec}",
    "watch-test": "mocha --recursive -w",
    "security": "nsp check --output summary"
  },
  "license": "UNLICENSED",
  "devDependencies": {
    "chai": "^4.1.2",
    "memfs": "^2.8.0",
    "mocha": "^5.0.4",
    "nock": "^9.2.3",
    "nsp": "^3.2.1",
    "nyc": "^11.4.1",
    "opener": "^1.4.3",
    "rimraf": "^2.6.2",
    "sinon": "^4.4.4"
  },
  "dependencies": {
    "isomorphic-unfetch": "^2.0.0",
    "p-queue": "^2.3.1",
    "standard": "^11.0.0"
  }
}
